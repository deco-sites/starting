import Component from "$store/components/camp/ui/SliderJS.tsx";
import type { Props } from "$store/components/camp/ui/SliderJS.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
