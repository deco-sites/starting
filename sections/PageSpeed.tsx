import PageSpeed, {
  Props,
} from "deco-sites/starting/components/camp/pageSpeed/index.tsx";

function PageSpeedSection(props: Props) {
  return <PageSpeed {...props} />;
}

export default PageSpeedSection;
